.venv/
__pycache__/
*.pyc

# Raw per-month GeoTIFFs are regenerated locally by modis_kenya_monthly_ingest.py
# and aren't needed by the dashboard (which only reads the CSVs / parquet cache).
kenya_monthly_ingest/*.tif

.streamlit/secrets.toml
